{
  "name": "leodis-relay",
  "private": true,
  "version": "0.0.0",
  "description": "Leodis Relay - site and compliance reporting platform",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "npm run build:packages && npm run build:web",
    "build:packages": "tsc --build",
    "build:web": "npm --workspace @relay/web run build",
    "typecheck": "tsc --build && tsc -p apps/web/tsconfig.json --noEmit",
    "test": "npm run build:packages && node --test \"packages/*/dist/**/*.test.js\" && npm --workspace @relay/web test",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  }
}
